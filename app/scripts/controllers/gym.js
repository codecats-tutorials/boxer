'use strict';

/**
 * @ngdoc function
 * @name projApp.controller:GymCtrl
 * @description
 * # GymCtrl
 * Controller of the projApp
 */
angular.module('projApp')
  .controller('GymCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
