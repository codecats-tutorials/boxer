'use strict';

/**
 * @ngdoc function
 * @name projApp.controller:TicketsCtrl
 * @description
 * # TicketsCtrl
 * Controller of the projApp
 */
angular.module('projApp')
  .controller('TicketsCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
